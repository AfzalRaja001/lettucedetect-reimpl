# Install PyTorch separately first, matching your machine's hardware:
# https://pytorch.org/get-started/locally/
# (CPU build on the dev machine, CUDA 12.x build on the RTX 4060 machine)

transformers>=4.48.0
datasets>=3.0.0
accelerate>=1.0.0
scikit-learn>=1.5.0
safetensors>=0.4.5
pyyaml>=6.0
numpy>=1.26.0
tqdm>=4.66.0
